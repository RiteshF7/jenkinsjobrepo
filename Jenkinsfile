pipeline {
 
agent { node { label 'master' } }
tools {
        jdk 'jdk'
    }
stages {
        stage('Code validate') {
            steps {
                sh 'echo 'this is build''
            }
        }
       
 
       
    }
}
